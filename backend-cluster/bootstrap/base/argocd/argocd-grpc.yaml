# Argo CD API server runs both a gRPC server (used by the CLI),
# as well as a HTTP/HTTPS server (used by the UI).
# Both protocols are exposed by the argocd-server service object on the following ports:
# 443 - gRPC/HTTPS
# 80 - HTTP (redirects to HTTPS)
apiVersion: v1
kind: Service
metadata:
  annotations:
    alb.ingress.kubernetes.io/backend-protocol-version: HTTP2 #This tells AWS to send traffic from the ALB using HTTP2. Can use GRPC as well if you want to leverage GRPC specific features
  labels:
    app: argogrpc
  name: argogrpc
  namespace: argocd
spec:
  ports:
  - name: "443"
    port: 443
    protocol: TCP
    targetPort: 8080
  selector:
    app.kubernetes.io/name: argocd-server
  sessionAffinity: None
  type: NodePort


# Use this annotation (which must match a service name) to route traffic to HTTP2 backends.
# metadata:
#   annotations:
#     alb.ingress.kubernetes.io/conditions.argogrpc: |
#  [{"field":"http-header","httpHeaderConfig":{"httpHeaderName": "Content-Type", "values":["application/grpc"]}}]

# add this to your ingress paths
#      - path: /
#        backend:
#          service:
#            name: argogrpc
#            port:
#              number: 443
#        pathType: Prefix

