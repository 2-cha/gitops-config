apiVersion: v1
kind: Service
metadata:
  name: demo-service
spec:
  selector:
    app: demo-service
    tier: backend
  ports:
    - port: 80
      targetPort: 8080 # containerPort
  externalTrafficPolicy: Local
  type: NodePort
