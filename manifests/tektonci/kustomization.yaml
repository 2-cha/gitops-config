apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: tektonci
resources:
  - crd
  - namespace
#  - secrets
  - rbacs
  - listeners
  - ingresses
  - pipelines
  - triggers/overlays # add all trigger under overlays
