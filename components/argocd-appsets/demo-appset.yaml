apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: demo-service
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  generators:
    - list:
        elements:
          - env: dev
  #          - env: stage # TODO: Deployment/demo-service is part of applications argocd/demo-service-prod and demo-service-stage...
  #          - env: prod  # TODO: gradually promote
  template:
    metadata:
      name: 'demo-service-{{env}}'
      namespace: argocd
      finalizers:
        - resources-finalizer.argocd.argoproj.io
    spec:
      project: default
      source:
        repoURL: git@github.com:2-cha/gitops-config.git
        targetRevision: main
        path: apps/backend/demo/overlays/{{env}} #TODO: kustomize
      destination:
        server: https://kubernetes.default.svc
        namespace: default
      syncPolicy:
        automated:
          prune: true
          selfHeal: true