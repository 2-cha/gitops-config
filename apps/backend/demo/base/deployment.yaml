apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-service
  annotations:
    revision: demo-74f0badc88c0ddb87732db9deabf2bcb7dfde1a2
spec:
  # ReplicaSet 정의
  replicas: 3
  selector:
    matchLabels:
      app: demo-service
      tier: backend
  # Pods 정의
  template:
    metadata:
      name: demo-service
      labels:
        app: demo-service
        tier: backend
    spec:
      containers:
        - name: spring-boot
          image: m1nq/my-first-app:demo-74f0badc88c0ddb87732db9deabf2bcb7dfde1a2
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
              protocol: TCP
      imagePullSecrets:
        - name: image-pull-credentials
